{
  "framework": "vite",
  "buildCommand": "cd frontend && npm run build && cd .. && cp -r frontend/dist .",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}