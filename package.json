{
  "name": "intelquiz",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build && cd .. && cp -r frontend/dist . && cp vercel.json dist/",
    "lint": "cd frontend && npm run lint",
    "preview": "cd frontend && npm run preview",
    "install-frontend": "cd frontend && npm install"
  },
  "workspaces": ["frontend"]
}